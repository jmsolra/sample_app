Para recuperar tu password haz click en el siguiente link:

<%= edit_password_reset_url(@user.reset_token, email: @user.email) %>

El link expirará en dos horas.

Si no solicitaste la recuperación de password, por favor ignora este email y la password no se modificará.